# The precedence is .env.local .env.[environment] .env
# Place common and non-sensitive variables in .env and environment specific variables in .env.[environment]
# Also override variables in .env.local for local development
# And override variables in .env.[environment] for environment specific variables

PORT=3301

TTS_DEFAULT_PROVIDER=azure
TTS_AZURE_API_KEY=
TTS_GOOGLE_API_KEY=
TTS_IBM_API_KEY=
